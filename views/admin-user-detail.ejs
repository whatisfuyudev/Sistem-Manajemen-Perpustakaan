<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    /* Reset some defaults */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, 
                   "Helvetica Neue", Arial, sans-serif;
      background-color: #f5f5f5;
      color: #333;
      line-height: 1.5;
    }

    .container {
      max-width: 800px;
      margin: 2rem auto;
      padding: 1.5rem;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    h1 {
      font-size: 1.75rem;
      margin-bottom: 1rem;
      padding-bottom: 0.5rem;
      border-bottom: 1px solid #e0e0e0;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
    }

    .field {
      display: flex;
      flex-direction: column;
    }

    .label {
      font-size: 0.875rem;
      color: #666;
      margin-bottom: 0.25rem;
    }

    .value {
      font-size: 1.125rem;
      font-weight: 500;
      color: #111;
    }

    .profile-picture {
      margin-top: 1rem;
      margin-bottom: 1rem;
      text-align: center;
    }

    .profile-picture img {
      width: 128px;
      height: 128px;
      object-fit: cover;
      border-radius: 50%;
      border: 2px solid #e0e0e0;
    }

    /* Utility for empty fields */
    .empty {
      color: #999;
      font-style: italic;
    }

    /* back buttons */
    .back-button {
      background: #6c757d;
      padding: 10px 20px;
      border-radius: 4px;
      color: #fff;
      text-decoration: none;
      display: inline-block;
      margin-bottom: 20px;
    }

    .back-button:hover {
      background-color: #5a6268;
    }

    .truncated-text {
      overflow-x: scroll;          /* 1. Hide overflowed text */ 
      white-space: nowrap;      /* 2. Prevent wrapping */ 
    }

    /* Button Group */
    .button-group {
      display: flex;
      gap: 12px;
      margin-top: 1rem;
      margin-bottom: 1rem;
      justify-content: center;
    }

    /* Base button style */
    .btn {
      padding: 10px 20px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      display: inline-block;
      transition: background-color 0.2s ease;
    }

    .btn-secondary {
      background-color: #ffc107;
      color: #fff;
    }
    .btn-secondary:hover {
      background-color: #e0a800;
    }
  </style>
</head>
<body>

  <div class="container">
    <a href="/admin/panel?tab=users" class="back-button">Back</a>

    <h1>User Details</h1>

    <div class="profile-picture">
      <img
        src="<%= user.profilePicture || '/public/images/profile-pictures/default.jpg' %>"
        alt="Profile Picture"
      >
    </div>

    <div class="grid">
      <div class="field">
        <div class="label">Name</div>
        <div class="value"><%= user.name %></div>
      </div>

      <div class="field">
        <div class="label">Email</div>
        <div class="value truncated-text"><%= user.email %></div>
      </div>

      <div class="field">
        <div class="label">Role</div>
        <div class="value"><%= user.role %></div>
      </div>

      <div class="field">
        <div class="label">Phone</div>
        <div class="value"><%= user.phone || '–' %></div>
      </div>

      <div class="field">
        <div class="label">Address</div>
        <div class="value"><%= user.address || '–' %></div>
      </div>

      <div class="field">
        <div class="label">Account Status</div>
        <div class="value"><%= user.accountStatus %></div>
      </div>

      <div class="field">
        <div class="label">Created At</div>
        <div class="value"><%= user.createdAt.toDateString() %></div>
      </div>

      <div class="field">
        <div class="label">Updated At</div>
        <div class="value"><%= user.updatedAt.toDateString() %></div>
      </div>
    </div>

    <!-- Action Buttons -->
    <div class="button-group">
      <button 
      id="modifyBtn" 
      class="btn btn-secondary" 
      data-user-id="<%= user.id %>">
        Edit
      </button>

    </div>

  </div>

  <script src="/public/js/admin-user-detailScript.js">

  </script>
</body>
</html>
